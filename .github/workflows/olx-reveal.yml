name: OLX reveal (Faza 2)
# Czysty fetch (bez przeglądarki). Throttle jest PER-IP (~5/IP) → rotacja proxy IP.
# Numer → leads_ingest_offer → auto-SMS.

on:
  workflow_dispatch:
    inputs:
      max:
        description: "Ile ogłoszeń z kolejki"
        default: "60"
      cap_per_ip:
        description: "Reveali na jedno IP (proaktywna rotacja pod limitem ~5)"
        default: "4"
      delay_ms:
        description: "Odstęp między revealami (ms)"
        default: "2500"
  schedule:
    - cron: "*/30 * * * *"   # co 30 min — start łagodny, żeby IP zdążyły ostygnąć; podkręcimy po pomiarze yieldu

concurrency:
  group: olx-reveal
  cancel-in-progress: false

jobs:
  reveal:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    defaults:
      run:
        working-directory: scraper/olx-reveal
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - run: npm install --no-audit --no-fund
      - name: reveal
        env:
          IPROYAL_PROXY: ${{ secrets.IPROYAL_PROXY }}
          OLX_FP: ${{ secrets.OLX_FP }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          MAX: ${{ github.event.inputs.max || '40' }}
          CAP_PER_IP: ${{ github.event.inputs.cap_per_ip || '4' }}
          MAX_IP_TRIES: "4"
          DELAY_MS: ${{ github.event.inputs.delay_ms || '2500' }}
        run: node revealer.mjs
      - name: Slack alert on failure
        if: failure()
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SRK: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          WF: ${{ github.workflow }}
        run: |
          payload=$(printf '{"text":":red_circle: *Bot numerów — FAIL*\\n*%s* padł na cronie\\n<%s|Otwórz log w GitHub Actions>"}' "$WF" "$RUN_URL")
          curl -sS -X POST "$SUPABASE_URL/functions/v1/scraper-alert" -H "authorization: Bearer $SRK" -H 'content-type: application/json' -d "$payload" || true
